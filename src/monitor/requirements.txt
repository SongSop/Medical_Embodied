# Medical_Embodied - 床位检测模块依赖
# 安装方式: pip install -r requirements.txt
# 注意: 需已安装 ROS2（如 Jazzy）及 rclpy、cv_bridge、sensor_msgs 等

# ============ PyTorch (请根据目标机器 CUDA 版本调整) ============
# CUDA 12.x:
torch>=2.2.0
torchvision>=0.17.0
# 如果没有 GPU，安装 CPU 版本:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu

# ============ YOLOv8 床位检测 ============
ultralytics>=8.0.0

# ============ CLIP 有人/无人判断 ============
open-clip-torch>=2.0.0

# ============ 图像处理 ============
# numpy：全仓与其它模块对齐 >=1.17.4；<2 与 ROS2 cv_bridge（numpy 1.x ABI）一致
opencv-python-headless>=4.8.0
Pillow>=9.0.0
numpy>=1.17.4,<2.0.0

# ============ CUDA 运行时 (GPU 环境) ============
nvidia-cuda-runtime-cu12>=12.1.0
nvidia-cudnn-cu12>=8.9.0

# ============ 人脸识别 (face_identify_server) ============
face_recognition>=1.3.0
dlib>=19.22.0

# ============ 通用工具 ============
tqdm>=4.65.0
PyYAML>=6.0
matplotlib>=3.7.0
pandas>=2.0.0
